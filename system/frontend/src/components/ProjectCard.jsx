// Implementation guide: see docs/plans/LEVEL-3-SYSTEM.md — "Project Dashboard"
/**
 * ProjectCard — Status card for a single project in the project grid.
 *
 * Shows:
 *   - Project name and status badge (Live / Paused / Error)
 *   - Model version (from MLflow)
 *   - Last pipeline run time and status
 *   - Key metric (e.g. "+14.7 km² urban expansion")
 *
 * See: docs/plans/LEVEL-3-SYSTEM.md — "Project Dashboard" ASCII mockup
 */

export default function ProjectCard({ project }) {
  // TODO: Implement
  // Props: project = { id, name, status, model_version, last_run, key_metric }
  return null;
}
